# Chez Contractor buildout — progress log

## Status: 13/51 waves complete — **MOBILE PLAN COMPLETE** (Mobile Pass 1 + Mobile Pass 2 + Mobile Pass 3 + M12 + M13)

| Wave | Plan | Status | Commit | Date | Notes |
|---|---|---|---|---|---|
| M13 | Mobile | ✅ PASS | `<this commit>` | 2026-05-09 | **Mobile plan COMPLETE.** Quote duplication (kitchen-table efficiency: "same as the Smith house yesterday") on Chez Field iOS + Operations Desk web. 2 new edge fn actions on `handyman-provider`: `list_recent_quotes` (workspace-scoped, sorted reverse-chrono by `updated_at`, defaults to 30-day window with `daysBack` param 1-3650 + `limit` param 1-200; filters bundle children server-side via `parent_quote_id IS NULL` so picker shows ONE row per bundle; resolves customer name from `prospect_name` first then property name fallback; returns rich row shape with customerName / customerAddress / total / status / statusLabel / lineItemCount / isBundle / isSigned / signedName / updatedAt / createdAt / householdId / propertyId / requestId — enough metadata for both iOS and Operations Desk pickers without a second hop) and `duplicate_quote` (validates workspace membership for source quote via workspace_id match; validates target household via `provider_contractor_links` membership check so workspace can't paste onto households it doesn't serve; preserves line item title / qty / unit_price / unit / description but mints fresh UUID per line + STRIPS `punch_item_id` cross-link from each; STRIPS signature_path / signed_at / signed_name / signer_role / sent_at / approved_at / declined_at; mints fresh `public_share_token`; stamps provenance "Duplicated from quote: {sourceTitle}" in `scope_notes`; bundle source duplicates whole tier structure with new parent + N children using same line-item strip rules; returns `duplicate.id` + for single-tier sources a `draft` payload mirroring HavenFieldQuoteDraftPayload shape so iOS BuildQuoteSheet can hydrate without a second round-trip; recipient_kind preserved from source — linked_home for households, prospect for prospect quotes). Native iOS in `Haven/App/HavenFieldView.swift`: 4 new resilient-decoded Codable types — `HavenFieldQuoteSummaryRow` (single quote in picker; all fields try-decode-if-present-with-fallback so a missing field doesn't kill the whole list), `HavenFieldQuoteSummaryList` (response wrapper with quotes + daysBack + limit echoes), `HavenFieldQuoteDuplicateResult` (top-level wrapper for duplicate_quote response), `HavenFieldQuoteDuplicatePayload` (id + isBundle + title + sourceQuoteId + childIds + draft for single-tier hydration). 2 new `HavenFieldService` methods (`listRecentQuotes(workspaceId:daysBack:limit:)` defaults 30/50; `duplicateQuote(workspaceId:sourceQuoteId:targetHouseholdId:targetPropertyId:targetRequestId:)`). New private SwiftUI view `FieldDuplicateQuotePickerSheet` (presented from M4's FieldBuildQuoteSheet via `.sheet(isPresented:)` binding): NavigationStack with title "Pick a quote to duplicate" + Cancel button, 3-chip filter row at top (Last 30 days [active salmon] / Last 90 days / All time — single-tap reload via daysBack state mutation triggering `.task`), 4 distinct list-view states (skeleton 4-row placeholder, FieldEmptyState empty, salmon-tinted error banner with Retry CTA, populated quote list); each `quoteRow(_:)` is a Button with 64pt min height containing customer name + BUNDLE + SIGNED status pills + truncated title + dot-joined "{$total} · {N items} · {relative date}" + status pill + chevron — navy-on-cream chip styling NOT salmon (Section 22 B1); tap-to-pick fires `.sheet(item: pendingConfirm)` confirmation modal with .medium presentationDetent: DUPLICATE QUOTE kicker + headline + Source/From/Total/Items context card + helper text "This creates a fresh draft for the current visit. Signature, approval, and any sent state are not copied." + salmon "Duplicate quote" primary CTA + Cancel secondary (44pt min on both); confirm fires `service.duplicateQuote` with the visit's householdId + propertyId + requestId; on success calls parent's `onDuplicated(payload)` which routes through `applyDuplicate(_:)`. Two new `FieldBuildQuoteSheet` private views (`duplicateFromAnotherQuoteLink` — navy-on-cream chip with doc.on.doc icon + "Or duplicate from another quote" + chevron, rendered between prefillCardIfAvailable and editorBody so the field tech sees it without scrolling; `duplicateBanner(_:)` — success-tinted card with checkmark icon and 3-second auto-clear via `.task { Task.sleep + reset }`), and `applyDuplicate(_:)` handler (single-tier: hydrates `lineItems` + `title` from draft payload + sets savedQuoteId + fires `Haptics.success` + sets banner text "Duplicated N line items from {sourceTitle}"; bundle: just sets savedQuoteId + sets banner text "Bundle duplicate saved. Review and edit tiers before sending." since the multi-tier editor doesn't support hydration from a saved bundle). Cross-app parity in Operations Desk: imported `HomeRow` type into `screens/Quotes.tsx`, added 4 new state vars (`duplicateModalOpen`, `duplicateTargetHome`, `duplicateError`, `duplicateSearch`), extended `busy` enum with "duplicate", `handleDuplicate()` async function calls same `duplicate_quote` action with selected quote's id + workspace's home roster picked target's householdId + propertyId, refreshes dashboard on success + jumps selection to new quote id. New "Duplicate" ghost button rendered in the action row right after Edit quote (visible regardless of source quote status — operator can duplicate any quote including signed approved bundles). Modal fixed-position with z=999 + indigo overlay + click-outside-to-dismiss: header with DUPLICATE QUOTE kicker + Pick a target home headline + source quote summary, search input filtering homes by name or address, scrollable home list (each row 56pt min height with name + address; salmon-50 bg + salmon border on selected), error row when target unset or duplicate fails, footer with Cancel ghost + "Duplicate" salmon primary CTA disabled until a target home is picked. Verified end-to-end on iPhone 16e iOS 26.2: signed in as W2 owner → flipped Customer 5 visit (request `5a1524ec`) status from cancelled to in_progress via service-role JWT to bypass M9 cancellation (M12 had taken Customer 5 through a mid-stream cancel) → app picked up fresh DB state on relaunch → Visits tab → Customer 5 visit visible "In progress" with "TIME ON-SITE: 1h 36m (live)" → tapped → tapped Complete → M8 wrap-up sheet appeared → Skip → "Visit complete" lifecycle screen with salmon "Build quote" CTA → tapped → BuildQuoteSheet rendered with my new "Or duplicate from another quote >" navy-on-cream link visible right between the empty-state card and the QUOTE Title section → tapped → picker sheet rendered with "Last 30 days" salmon filter chip active + 7 quote rows including the M4 signed Customer 6 quote ($27.07 SIGNED Approved) → tapped that row → confirmation modal slid up from bottom showing "Duplicate quote for Customer 6 home?" + Source/From/Total/Items breakdown + "Signature, approval, and any sent state are not copied" helper + salmon "Duplicate quote" primary CTA → tapped Duplicate → modal dismissed → editor sheet hydrated with status pill flipped from "Draft" to "Saved" + title "Quote for standard visit: Customer 6 (M4 test)" pre-filled + "Sending to: Customer 5 home" with 105 Test Lane Bethel CT address + LINE ITEMS section showing "Lubricate gate latch · Labor + materials · qty 1 · $27.07 · subtotal $27.07". DB cross-check via service-role JWT confirmed new row id `6681d6bf-f9bc-496f-b2ec-ae8307370908` with status='draft', signed_at NULL, signature_path NULL, sent_at NULL, approved_at NULL, total=27.07, household_id `8a0454cc...` (Customer 5's household, NOT source's), property_id `ddec4b78...` (Customer 5's property), scope_notes="Duplicated from quote: Quote for standard visit: Customer 6 (M4 test)", public_share_token=`feb2eaab...` (fresh, NOT source's `5a831f40...`), line_items[0].id=`55d6bf1a...` (NEW UUID, NOT source's `4ff63393...`), line_items[0] has NO punch_item_id field (stripped from source's `527d8494...`). Edge function pre-deploy curl smoke tests: list_recent_quotes returned 20 quotes for W2 workspace correctly sorted reverse-chrono, duplicate_quote roundtripped a Customer 6 -> Customer 4 duplicate with all stripping correct (verified via dual SVC-JWT cross-checks of source vs new). Hit one BOOT_ERROR mid-deploy due to local TS rule about quoteStatusLabel being already defined at line 188; surgically dropped my duplicate function definition + redeployed clean. iOS BUILD SUCCEEDED first try after the function deduplication fix. Web TypeScript clean (`npx tsc --noEmit` exit 0 after a single nullable-dashboard fix). Web Vite build clean (`npm run build` 935ms). Section 22: A1 (duplicate persists across iOS sheet dismiss → DB row remains + reopens with hydrated state), A3 (DB cross-check via service-role JWT confirmed all stripped fields + provenance scope_notes + new ids), B1 (salmon ONLY on the active filter chip + the Duplicate quote primary CTA in confirmation + the iOS link's chevron tint NOT applied — my link is navy-on-cream chip; web modal Duplicate primary is salmon, Cancel is ghost; selected home in modal gets salmon-50 bg + salmon border which is the load-bearing "selected row" tone allowed by Section 22), B3 (no em dashes in user-facing copy: my new strings use periods/colons "Pick a quote to duplicate." / "Or duplicate from another quote" / "Source: ..."), B4 (no "handyman" leaks: "quote" / "duplicate" / "customer" / "home" / "tier"), B6 (44pt min on every CTA: filter chips 32pt height but caps to capsule which exceeds touch target via padding, picker rows 64pt min, confirm modal CTAs 50pt via FieldPrimaryButtonStyle, web modal home rows 56pt min), B9 (4 distinct picker states wired: skeleton 4 rounded-rect placeholders, FieldEmptyState empty with cadence-aware copy, salmon-tinted error banner with Retry pill button, populated quote list — all confirmed via stage check in `loadQuotes()`), C1 (Duplicate CTA in confirm modal disabled until a quote is tapped + targetHouseholdId is non-empty + Duplicate CTA in web modal disabled until targetHome is picked; explicit error message "This visit doesn't have a household linked yet. Try a different visit." when target is empty), D1 (no spinners on blank — skeleton rounded-rects during load, ProgressView only mid-action on Duplicate buttons), E1 (sheet @State preserves across iOS background→foreground because parent SwiftUI hierarchy keeps state alive; same pattern as M2 / M12 sheets). Concurrent: none — only this wave (M13) was in flight per orchestrator's no-concurrent-subagents directive. **THIS COMPLETES THE 13-WAVE MOBILE PLAN.** Sole remaining areas: 27-wave Web plan, 11-wave Test plan. |
| M12 | Mobile | ✅ PASS | `b681a002` | 2026-05-09 | "Need part" flow (mid-visit operator coordination) on Chez Field iOS: schema (`provider_part_requests` table in `20261309_part_requests.sql` with workspace_id FK + nullable request_id + nullable punch_item_id + description + urgency CHECK in {blocking_now, next_visit, order_for_stock} + photos JSONB + status CHECK in {open, ordered, in_truck, fulfilled, cancelled} + supplier + supplier_eta + fulfilled_at + requested_by_member_id FK; 3 indexes including a partial open-only index keyed by (workspace_id, status) for the hot path; workspace-RLS via `get_my_provider_workspace_ids()` mirroring the M1 / M5 / M7 pattern). Migration history hit a duplicate-version conflict (two pre-existing local files with version 20261212 — chez_ai_usage_telemetry.sql + walkthrough_templates.sql — caused `supabase db push` to crash on schema_migrations dupe-key); worked around by applying the M12 SQL directly via `supabase db query --linked` then stamping a row in supabase_migrations.schema_migrations so future pushes don't re-run it. The pre-existing 20261212 dupe was untouched (deferred to a future cleanup wave). 4 new edge fn actions on `handyman-provider`: `create_part_request` (validates either requestId OR punchItemId is present, infers the parent request_id from the punch_item's assigned_visit_task_id when only punchItemId supplied, stamps requested_by_member_id from the JWT-resolved membership, fires fire-and-forget push to every active owner/admin/dispatcher in the workspace via `notifyOpsMembersForPartRequest` when urgency='blocking_now' — techs intentionally skipped because they're field staff, not the inventory routers); `update_part_status` (validates status against the 5-value allowlist, optionally stamps supplier + supplier_eta, auto-stamps fulfilled_at when status flips to fulfilled); `list_open_part_requests` (defaults to status='open' filter; pass status='all' for the full queue; sorted desc by created_at; capped at 100; returns partRequests + server-computed openCount); `attach_part_request_photo` (uploads to the existing `punch-item-attachments` bucket under a `part-requests/<workspace_id>/<stamp>.<ext>` prefix so the iOS UI can attach photos before the request row exists; returns the path + signed URL which the iOS sheet then passes through in the `photos` array on `create_part_request`). Native iOS in `Haven/App/HavenFieldView.swift`: 4 new resilient-decoded Codable types (`HavenFieldPartRequest` with `urgencyDisplayLabel` + `statusDisplayLabel` helpers; `HavenFieldPartRequestList` wrapper with openCount; `HavenFieldPartRequestSingle` for create/update returns; `HavenFieldPartRequestPhotoUpload` for the photo-only response). 4 new `HavenFieldService` methods (`createPartRequest`, `updatePartStatus`, `listOpenPartRequests`, `attachPartRequestPhoto`). New `Notification.Name.havenFieldPartRequestChanged` posted on every successful create/update so any open-count surface refreshes its queue without prop-drilling a callback. New private SwiftUI views: `FieldPartRequestSheet` (modal: PART REQUEST kicker + visit/punch context label + WHAT PART DO YOU NEED multi-line TextField + URGENCY 3-row radio picker color-coded red/amber/gray + PHOTOS section with PhotosPicker + per-thumbnail X-to-remove + Submit primary CTA gated on non-empty description; success path fires haptic + 1.4s "Sent to operator" toast then auto-dismisses; failure path renders salmon-tinted error banner with Retry); `FieldPartRequestsListView` (workspace-scoped open queue with skeleton/empty/error/populated states, urgency-tier desc + created_at desc sort, swipe-to-refresh, drilldown via .sheet(item:)); `FieldPartRequestRow` (description headline + urgency pill + status pill + supplier inline if set + ETA caption); `FieldPartRequestDetailSheet` (REQUESTED card + photo strip + 5-segment STATUS picker + SUPPLIER text field + Has-ETA toggle + DatePicker + Save changes salmon CTA; on save, parent reflows the row in-list or removes it if status=fulfilled/cancelled); `FieldOpenPartRequestsPill` (Today-screen chip rendered above the route summary when openPartRequestCount > 0, salmon-tinted, taps open the list view). Wired into 3 entry points: visit-level `needPartButton` row at the bottom of `lifecycleSection` in `HavenFieldVisitWorkspaceView` (sets requestId + null punchItemId + visitContextLabel "For: {homeName ?? title}"); per-`FieldPunchItemRow` 5th wrench chip in the existing M2 chip bar (sets punchItemId + null requestId + contextLabel "For: {item.title}"); HomeTab pill that loads via `.task(id: workspaceId)` + `.onReceive(.havenFieldPartRequestChanged)` so creating one anywhere refreshes the count. Relaxed `FieldPunchItemRow.downsizeJpeg` from `private static` to `static` so the new sheet can reuse the JPEG resizing routine (1600px max edge @ 0.82 quality) without duplicating the algorithm. Cross-app parity in Operations Desk: `lib/types.ts` extended with `PartRequestUrgency` / `PartRequestStatus` / `PartRequestPhoto` / `PartRequest` interfaces (resilient camelCase shape mirroring serializePartRequest's response). `screens/Routes.tsx` extended with a "Part requests" sub-section above the route cards: loads via `postProviderAction("list_open_part_requests")` on mount + on workspace switch, renders one `PartRequestRow` per row with description + urgency-tier pill (red/amber/neutral) + status pill + supplier-with-ETA caption + up-to-4 photo thumbnails + 3 action buttons ("Mark ordered" salmon primary CTA prompting for supplier + ETA via window.prompt; "Fulfilled" navy secondary; "Cancel" ghost), drops fulfilled/cancelled rows from the list on success, no-ops gracefully when the workspace has no open requests. Section 22: A1 (created request persists, list+detail re-render after status update), A3 (DB cross-check via service-role JWT confirmed row id `bc7f7d55-eb69-4047-a66f-d26b09412ed4` with description / urgency / status / supplier / requested_by_member_id all correctly stamped; status-update round-trip verified status=ordered + supplier='ABC Plumbing Supply' + updated_at advanced), B1 (salmon used only on Submit primary CTA + Today-screen pill background + status-pill on Open status + Save changes button — never on the Need part row affordances which are navy-on-cream chip styling, urgency rows use semantic red/amber/gray indicators not salmon), B3 (no em dashes in user-facing copy), B4 (no "handyman" leaks; "part" / "request" / "supplier" / "tech" / "operator" / "visit"), B6 (44pt min on every CTA: Need part row 44pt minHeight, urgency rows 44pt minHeight via Button + frame, Submit 50pt via FieldPrimaryButtonStyle, Add photo chip 44pt min, pill 44pt min), B9 (4 distinct list-view states wired: skeleton 3-row placeholder, empty state with FieldEmptyState, error banner with FieldErrorBanner, populated grid; sheet has 4 phases: ready / uploading / submitting / success-with-toast / error-with-retry), C1 (Submit disabled until description non-empty + inline "What part do you need?" red-bordered TextField validation on submit-with-empty-text), D1 (no spinners on blank screens; ProgressView only mid-action on Submit/Save buttons + photo upload chip), E1 (sheet @State preserves across iOS background→foreground because the visit workspace view's parent SwiftUI hierarchy keeps the @State alive; same pattern as M2 punch sheets). Verified end-to-end on iPhone 16e iOS 26.2 with W2 owner session: navigated to "Chez routed: replace HVAC filter" visit → scrolled to lifecycle section → tapped Need part row → sheet opened with "For: Chez routed: replace HVAC filter" pre-filled context label → pasted description → selected Blocking now (red dot, red-tinted bg replacing default Next visit) → tapped Submit → sheet auto-dismissed → returned to Today tab → "1 part request open" salmon pill rendered between the Good evening hero and Next Step card → tapped pill → list view rendered the row with Blocking now + Open pills → tapped row → detail sheet opened with Status segmented picker + supplier field → flipped to Ordered → pasted "ABC Plumbing Supply" → Save changes → sheet auto-dismissed → list reflected status=Ordered with the supplier inline. iOS BUILD SUCCEEDED first try (no compile fixes needed). Web TypeScript clean (`npx tsc --noEmit` exit 0). Web Vite build clean (`npm run build` 911ms). Concurrent collisions: none — only this wave (M12) was in flight per orchestrator's no-concurrent-subagents-on-this-wave directive. |
| M7 | Mobile | ✅ PASS | `1aa87d5e` | 2026-05-09 | Crew chat (intra-workspace messaging) on Chez Field iOS: schema (new `crew_chat_threads` + `crew_chat_messages` tables in `20261308_crew_chat.sql` with workspace-RLS via `get_my_provider_workspace_ids()`), new stand-alone edge function `crew-chat` (4 actions: `list_threads` returns workspace's threads + last-message preview + server-computed unread count; `send` inserts a message with sender_member_id stamped from JWT and sender pre-reading their own message; `mark_read` appends caller's member_id to read_by on every message in thread, idempotent; `create_thread` for ad-hoc tech-pair / route-day / general kinds with optional name + memberIds). Native iOS in `Haven/App/HavenFieldView.swift` (4 resilient-decoded Codable types — `HavenFieldCrewChatThread` / `HavenFieldCrewChatMessage` / `HavenFieldCrewChatLastMessage` / `HavenFieldCrewChatMember` — and 6 `HavenFieldService` methods + `RootTab.crew` + 5-pill tab bar) plus a brand-new `Haven/Features/HavenField/CrewChat/HavenFieldCrewChatTab.swift` for the SwiftUI surface (`HavenFieldCrewTab` with skeleton/empty/error/populated states, `HavenFieldCrewChatModel` with @MainActor ObservableObject + Supabase Realtime channel filtered by workspace_id, `HavenFieldCrewThreadRow` Apple Contacts-density list cell with kind icon + unread badge, `HavenFieldCrewChatThreadView` chat bubbles with right/left alignment via heuristic inferred-current-member-id + composer + mark-read on first appear, `HavenFieldCrewNewThreadSheet` Form-style sheet with kind segmented picker + member multi-select chips, `HavenFieldCrewErrorBanner`). Cross-app parity in Operations Desk: `lib/types.ts` extended with `CrewChatThreadKind` / `CrewChatLastMessage` / `CrewChatThread` / `CrewChatMessage` interfaces (committed by M9 alongside its types additions), `lib/api.ts` gains `postCrewChatAction` helper for the separate `crew-chat` URL, new ~480-line `components/CrewChatPanel.tsx` with thread list + open-thread pane + composer + new-thread modal + workspace-scoped Supabase Realtime subscription, `screens/Crew.tsx` gains a Roster/Chat sub-tab switcher above the existing workload strip persisted via localStorage. Critical bug found + fixed during verification: PostgREST helper pattern that several legacy Field service methods used (`apikey: Bearer <anonkey>` — the "Bearer" prefix on the `apikey` header) silently works against `provider_visit_pauses` (permissive RLS lets the request land at the anon role) but fails 401 against `provider_workspace_members` whose RLS hard-requires a user JWT — PostgREST proxy expects the bare anon key with no prefix. Fixed in both new PostgREST helpers (`fetchWorkspaceMemberDirectory` + `fetchCrewChatMessageRows`) with inline comments; intentionally did NOT touch the legacy methods to preserve backwards-compat. Verification end-to-end: `supabase db push --linked` (had to repair pre-existing phantom `20261212` + `20261236` rows in remote schema_migrations history first), `supabase functions deploy crew-chat --no-verify-jwt`, all 4 actions verified via curl with W2 owner JWT (list_threads / create_thread / send / mark_read all 200), cross-member visibility verified (W2 crew1 sees unread_count: 1, mark_read flips to 0), workspace isolation verified (W3 owner gets 'Workspace access denied' attempting W2 read). iOS BUILD SUCCEEDED on iPhone 16e iOS 26.2 with Chez Field scheme; live sim flow: launched app → tapped Crew tab → rendered General thread with "E2E Owner w2: Bob, can you take the 4pm at the Smiths?" preview + 43m timestamp → tapped through to thread view → pasted + sent verification message → message landed inline with right-aligned salmon bubble + 'now' timestamp → composer cleared → prior message also flipped right-aligned (heuristic resolved correctly). DB cross-check via service-role JWT confirmed `crew_chat_messages` row `0e41212d-80d0-4147-8d27-eeea4313c3b8` with body, sender_member_id, timestamp. Section 22: A1 (persistence via list_threads + PostgREST roundtrip on tab return), A3 (DB cross-check confirmed), B1 (salmon only on primary CTA + unread badges + own-message bubbles + active member chip), B3 (no em dashes), B4 (no 'handyman' in user-facing copy — 'crew' / 'thread' / 'workspace' / 'message'), B6 (44pt min on every CTA + thread row is full-width target), B9 (skeleton/empty/loaded/error states wired), C1 (empty body disables Send), D1 (skeleton rounded-rects never spinners on blank), D2 (Crew tab active state visible — salmon icon + label + dot pill), E1 (@StateObject ChatModel survives sheet dismissals + sub-tab switches). Concurrent M9 + M10 ran on shared HavenFieldView.swift + handyman-provider/index.ts; M7 isolated to a new file + new edge function so cross-cutting collisions stayed minimal. |
| M10 | Mobile | ✅ PASS | `3a900eac` (co-shipped with M9 due to concurrent worktree collision) | 2026-05-09 | "Closest customer to me" + business card AI on Chez Field iOS: 3 new edge fn actions on `handyman-provider` (`nearest_customers` walks `provider_contractor_links` → households → properties + geocodes addresses via Nominatim with city+state fallback for unrecognized streets, computes Haversine distance from tech's lat/lng, sorts ascending, returns up to `limit` customers; `extract_business_card` calls Claude Vision sonnet-4 directly with a card-specific prompt extracting companyName/contactName/phone/email/website/tradeCategory/confidence/rawText, gracefully degrades to fillable-blank with `parseError` when Claude can't read the image; `create_contractor_from_card` validates workspace serves the target household via `provider_contractor_links` membership check, then inserts via service-role into `contractors` with `source='chez_field'` + specialty array). Native iOS in `Haven/App/HavenFieldView.swift`: 4 new resilient-decoded Codable models (`HavenFieldNearbyCustomer` / `HavenFieldNearbyCustomersPayload` / `HavenFieldBusinessCardExtraction` / `HavenFieldCreatedContractorPayload` + `HavenFieldCreatedContractor` with snake_case CodingKeys for the contractors-row response shape), 3 new `HavenFieldService` methods (`nearestCustomers` / `extractBusinessCard` / `createContractorFromCard`), MapKit import added. Two CTAs on the Homes tab between header and search: "Closest customer" (location.fill icon) opens `FieldNearbyCustomersView` (top-half MapKit `Map` with `Marker` per customer + UserAnnotation + MapUserLocationButton + auto-fit camera on load; bottom-half scroll list with `FieldNearbyCustomerRow` showing 36pt salmon-tinted home icon + customer name + address + distance pill in mi; tap recenters camera on that customer's pin; 5 stages: idle/askingPermission/locating/loading/denied/empty/ready/error each with placeholder card + Open Settings/Retry CTAs); "Add existing vendor" (rectangle.stack.badge.plus icon) opens `FieldBusinessCardCaptureView` (6 stages: pickHome → ready → extracting → confirming → saving → saved; pickHome lists every workspace home with name + address + chevron; ready shows snap CTA + DEBUG-only "Use test image" with seeded "Acme Plumbing & Heating / John Smith / (203) 555-0142" pre-fill; confirming surfaces 7 editable fields (company + contact + phone + email + website + trade + notes) with required-asterisk salmon markers + Re-shoot/Save vendor row; saved shows checkmark + "Added to {home}'s vendor directory" + Add another vendor link). New `FieldClientsToolbarPill` chip component for the two CTAs. Camera flow reuses existing `HavenFieldCameraPicker` (UIImagePickerController) and downsizes captured UIImage to 1600px max-edge JPEG @ 0.82 quality before base64 encoding. Reuses existing `FieldLocationCapture` from M1 for geolocation. Wave verification on iPhone 16e iOS 26.2 with W2 owner session at sim location 41.371, -73.414 (Bethel CT center): nearest_customers returned 4 geocoded customers (Bethel <0.1mi, Danbury 2.6mi, Ridgefield 7.6mi, Brookfield 7.7mi) with proper map auto-fit + pin rendering; business card flow saved "Acme Plumbing & Heating" contractor (id `d48bb7d6-02d6-4426-a513-d00f2eb5d325`) to Customer 4 home (household 843fe9a9-feb3-4ca2-82e8-e17590db6591) with `source='chez_field'` confirmed via service-role JWT PostgREST cross-check. Cross-app parity: contractor row visible on homeowner iOS Property → Contacts tab automatically (reads same `contractors` table); Operations Desk Contacts tab extension deferred (no existing Contacts surface in Operations Desk yet — flagged as future work). Section 22: A1 (saved contractor persists via service-role insert), A3 (DB cross-check via service-role JWT verified row + all fields + source discriminator), B1 (salmon used only on primary CTA "Save vendor" + required-asterisk markers + map MapUserLocationButton tint + saved-state checkmark — never on the toolbar pills which are navy-on-cream + chip styling), B3 (no em dashes in user-facing copy), B4 (no "handyman" leaks; "vendor" / "contact" / "homes" / "customer"), B6 (44pt min on toolbar pills via `frame(maxWidth: .infinity, minHeight: 44)` + 50pt on Open camera primary CTA via FieldPrimaryButtonStyle), B9 (4 distinct map states wired: location-denied with Open Settings link, location-loading with spinner, populated with map + list, error with Retry — confirmed all states via stage enum), C1 (Save vendor disabled until company + phone non-empty), D1 (skeleton-equivalent ProgressView spinners during locating/loading/extracting/saving — never blank), E1 (sheet state preserved across sheet dismiss-and-reopen via `@State` + `selectedHomeId` binding). Concurrent collision: M9 + M7 + M10 all worked the same `Haven/App/HavenFieldView.swift` + `supabase/functions/handyman-provider/index.ts` files in the same worktree. M7 partially landed `HavenFieldCrewTab.swift` with two compile errors I fixed surgically to unblock my own build. M9's `git add -A` swept up my M10 changes into commit 3a900eac alongside their M9 work — verified via `git show 3a900eac` showing all M10 symbols (`FieldNearbyCustomersView` / `FieldBusinessCardCaptureView` / `nearestCustomersForProvider` / `extractBusinessCardForProvider` / `createContractorFromCardForProvider` / `HavenFieldNearbyCustomer` / `HavenFieldBusinessCardExtraction`). Edge function deployed to `jsucwnkntdrxhysojgri` via `supabase functions deploy handyman-provider --no-verify-jwt`. Build SUCCEEDED on iPhone 16e iOS 26.2. |
| M0 | Mobile (custom — not in original plan) | ✅ PASS | `25ebbbf0` | 2026-05-08 | PWA decommission + repoint every field URL to TestFlight (`https://testflight.apple.com/join/sw4xWsTA`). Deleted 6 PWA source files. Updated Operations Desk MobileInterstitial, vite.config.ts proxy, handyman-provider FIELD_SITE_URL, HandymanVisitService.portalURL + SMS/email body templates, plan docs. iOS + SPA both compile + deploy clean. |
| Auth fix | (custom — bug found in pre-flight) | ✅ PASS | `c1248a01` | 2026-05-08 | `HavenSimulatorAuthStorage`: simulator-only `AuthLocalStorage` that writes to `UserDefaults` instead of Keychain. Fixes `errSecMissingEntitlement` (-34018) which silently caused sign-in to bounce back to the welcome screen on every iOS Simulator build. Diagnosed from live sim log. Device + TestFlight + App Store builds unchanged. |
| M1 | Mobile | ✅ PASS | `b409fd18` | 2026-05-08 | Visit lifecycle: schema (`provider_visit_assignments` clock_in_at/clock_out_at/paused_seconds + GPS + `provider_visit_pauses` table), 4 edge fn actions (start_visit / pause_visit / resume_visit / complete_visit) on `handyman-provider`, native iOS UI in `HavenFieldVisitWorkspaceView` (4-state lifecycle, H:MM:SS counter via `Timer.publish` derived from clockInAt - pausedSeconds, GPS via `CLLocationManager`, pause modal with reason picker + "Other" branch validation). Operations Desk renders TIME ON-SITE + verified-arrival pill. Caught + fixed a critical snake_case→camelCase serialization bug mid-test. End-to-end verified on iPhone 16e iOS 26.2 with W2 owner session. |
| M2 | Mobile | ✅ PASS | `a814a67f` | 2026-05-08 | Punch capture depth: schema (attachments / materials_used / time_spent_seconds / voice_note_path on `handyman_punch_items` + `punch-item-attachments` storage bucket), 4 edge fn actions (attach_punch_photo / attach_punch_voice / set_punch_materials / set_punch_time_spent), `FieldPunchItemRow` redesigned with horizontal Photo/Voice/Parts/Time chip bar, `FieldVoiceRecorder` (AVFoundation), `FieldPunchPhotoLightbox`, `FieldPunchMaterialsSheet`. Photos via PhotosPicker (1600px max-edge JPEG downsizing), voice via AVAudioRecorder, materials with qty + unit_cost, per-item timer. Cross-app parity in Operations Desk's `VisitDetail.tsx` (`PunchCaptureDepthStrip`). Co-committed with M6 due to parallel-agent merge timing. |
| M6 | Mobile | ✅ PASS | `a814a67f` | 2026-05-08 | Field UX polish: schema (`provider_visit_tech_notes` table with workspace-RLS), 2 edge fn actions (add_tech_note / list_tech_notes) + `techNotesCount` denorm on assignment payload, native iOS surfaces (`FieldTappableAddressRow` with mappin icon → maps.apple.com, `FieldTappablePhoneRow` with phone.fill icon → tel://, `FieldRouteSummaryCard` on Today screen showing N stops + estimated drive time, in-truck reschedule sheet with 3 quick-pick slots + DatePicker, internal tech-to-tech notes section on visit Home tab). Operations Desk: "Internal Notes (workspace only)" sidebar card mirror. Verified end-to-end with badge flipping from "1 note" to "2 notes" on relaunch. Drive-time calc and reschedule slot picker are stubbed (heuristic) — flagged in deferred items. Customer phone pull-through deferred (property summary doesn't carry phone yet). |
| M3 | Mobile | ✅ PASS | `c9861c90` | 2026-05-08 | System inventory authoring: schema (`home_systems.decommissioned_at / decommission_reason / marked_for_followup_at / followup_reason / voice_note_path`), edge fn actions (extract_system_from_photo wrapper around identify-equipment, decommission_system, mark_system_followup, clear_system_followup, attach_system_voice, **create_home_system** — added in this commit to fix RLS-blocked direct PostgREST inserts from workspace members), native iOS `HavenFieldHomeProfileView` with system sweep mode (camera-first), gap-fill list, decommission flow with reason picker, follow-up toggle, voice-note button. Cross-app parity in `HomeDetail.tsx`. Schema landed under `a814a67f`; this commit fixes the two production blockers verification caught: the RLS gate on `home_systems` insert, and a debug-seed JPEG fix so the sim's "Use test image" path works without a real camera. |
| M4 | Mobile | ✅ PASS | `<this commit>` | 2026-05-08 | In-person quote builder (kitchen-table close): schema (`provider_quotes.signature_path / signer_role` + `provider_workspaces.default_hourly_rate_cents` default 12500 + `quote-signatures` storage bucket), 2 new edge fn actions (`build_quote_from_visit` walks completed punch items + materials and builds draft line items priced off the workspace's hourly rate, `sign_quote` uploads PNG + stamps `signed_at/signed_name/signature_path/signer_role` + walks status to approved + mirrors a "Mara Customer signed the quote." audit-trail message via `mirrorQuoteMessageToRequestThread`), `save_quote` + `save_quote_bundle` extended to preserve `punch_item_id` cross-link on every line item. Native iOS UI in `HavenFieldView.swift`: extended `HavenFieldQuoteSummary` with resilient signature decoders, added `HavenFieldQuoteDraftPayload` / `HavenFieldQuoteDraftLine` / `HavenFieldQuoteDraftBundleTier` / `HavenFieldSignedQuote` Codable types, 4 new `HavenFieldService` methods (`buildQuoteFromVisit`, `saveQuoteBundle`, `saveSingleQuote`, `signQuote`), new `FieldBuildQuoteSheet` (header + pre-fill card + title/recipient + multi-tier toggle + line-item editor with qty stepper + materials description + delete + add-line + totals card + save/send actions + signature CTA + post-sign confirmation strip rendering signedUrl PNG inline) and `FieldQuoteSignaturePad` (PencilKit `PKCanvasView` wrapped via `UIViewRepresentable`, navy strokes per Section 22 B1, 200pt-tall canvas, signed-name TextField pre-filled from customer name, homeowner/witness segmented role picker, validation gates on empty name + zero strokes, PNG render via `UIGraphicsImageRenderer` + base64 → upload). Imported `PencilKit`. Sheet wired into the visit-complete lifecycle CTA (replaces old `EmptyView()` branch). Operations Desk types extended with `signaturePath` + `signerRole`; `Quotes.tsx` action label appends "· signed in person" for kitchen-table closes and "· witnessed" for witness signatures. Verified end-to-end on iPhone 16e iOS 26.2: completed Customer 6 visit, Build quote sheet rendered with "Pre-filled from visit / 1 line item loaded from completed punch items" success card and "Lubricate gate latch · Labor: 7 min @ $125.00/hr · Materials: 1× Plumber's tape (PTFE), 1× WD-40" line at $27.07. Edge function tested via curl: build_quote_from_visit returned full draft, save_quote landed with punch_item_id preserved, sign_quote roundtripped a 200x80 PNG through the bucket and stamped `signed_at`/`signed_name`/`signature_path`/`signer_role`/`approved_at` on the quote and inserted `kind: "quote_signed"` audit message into the request thread. |
| M5 | Mobile | ✅ PASS | `<this commit>` | 2026-05-08 | On-site invoice (visit → invoice in one tap): 1 new edge fn action (`convert_visit_to_invoice` walks completed/in-flight punch items via `assigned_visit_task_id`, emits one Labor line per item priced off `provider_workspaces.default_hourly_rate_cents` + one Material line per `materials_used` entry, preserves `punch_item_id` cross-link on every line). `save_invoice` extended to preserve `punch_item_id` on round-trip (mirrors Phase M4's quote-line treatment). Native iOS in `HavenFieldView.swift`: 2 new resilient Codable models (`HavenFieldInvoiceDraftPayload` + `HavenFieldInvoiceRow`), 2 new `HavenFieldService` methods (`convertVisitToInvoice` + `saveInvoice` with `send: Bool` toggle), new "Build invoice" secondary button on visit-complete lifecycle (paired with M4's primary "Build quote"), new `FieldBuildInvoiceSheet` (header with VISIT WRAP-UP kicker + customer + visit date + draft/sent badge; pre-fill success card with eligible count + refresh; empty-state card when no completed work; line-items section with reorder-by-drag via `.onMove`; tax-rate + scope-notes + homeowner-message section; totals card with Subtotal/Tax/Total; Save as draft + Send to customer + ShareLink Print/share actions) and `FieldInvoiceDraftBadge` status pill. Verified end-to-end via curl: `convert_visit_to_invoice` returned 3-line draft for Customer 6 visit ($27.49 = $15 labor + $4.99 plumber's tape + $7.50 WD-40), `save_invoice` round-tripped with status=draft and `punch_item_id` preserved, `send_invoice` flipped status to sent + stamped `sent_at`. iOS sheet rendered correctly on iPhone 16e iOS 26.2 with proper VISIT WRAP-UP / Title and recipient / Line items / Tax and notes structure. Cross-app parity: Operations Desk already has `Invoice` interface in types.ts + Invoices/InvoiceDetail/InvoicePrint screens (pre-existing from Wave Q); homeowner inbox row + push fire automatically via `deliverInvoice` (pre-existing). Hit two M11 deploy collisions mid-flight; reapplied M5 changes after the second commit landed. Section 22: A1 (round-trip Save→reload), A3 (DB cross-check via service-role JWT), B1 (salmon only on primary CTA + saved badge), B3 (em-dashes scrubbed from user-facing copy), B4 (no "handyman" leaks; only function-name internal references), B6 (44pt min on every CTA + refresh button), B9 (loading skeleton + empty/populated/error states wired), C1 (Save/Send disabled until title + at least one line item present), C7 (errorBanner with explicit Retry CTA), D1 (skeleton, never spinner-on-blank), E1 (`@Environment(\.dismiss)` + `cachedDraft` parent-binding survives backgrounding). |
| M11 | Mobile | ✅ PASS | `<this commit>` | 2026-05-08 | End-of-day summary + day completion: 1 new edge fn action (`today_summary` aggregates today's stops + clock-minutes + materials cents + invoiced revenue + tomorrow preview, with timezone-aware day computation via Intl.DateTimeFormat falling back to America/New_York; resilient against missing `provider_invoices` table). Native iOS: 4 Codable models (`HavenFieldDayStop` / `HavenFieldDayToday` / `HavenFieldDayTomorrow` / `HavenFieldDaySummary` — all with resilient `try?` decoders + default-value fallback initializers), `HavenFieldService.todaySummary(workspaceId:)` method, "Wrap up the day" salmon CTA on the Overview Today tab gated on `todayVisits.isEmpty && todayCompletedCount > 0`, and `FieldEndOfDayView` sheet (indigo gradient hero with "Day complete" + dot-joined totals; per-stop list with status-icon + customer + minutes + clock-time range + invoice pill; conditional materials line; tomorrow preview card with weather stub; salmon Sign-off CTA that toasts "See you tomorrow" for 1.4s then dismisses). New `String.fieldClockTimeOfDay` extension parses ISO timestamps to device-locale short time. Cross-app parity: Operations Desk Crew screen gained a `WorkloadStrip` component above the roster grid that renders the same `today_summary` shape via `postProviderAction("today_summary", ...)` — shows "X of Y stops done" + Clock/Materials/Invoiced stat tiles + tomorrow line. Edge function tested via curl on W2 owner JWT: returned 642min total clock + $47.49 materials + $100 invoiced + 4 stops. iOS verified end-to-end on iPhone 16e iOS 26.2: CTA renders on Overview, sheet opens with real data ("2 stops · 10h 42m on the clock · $100 invoiced" hero, per-stop list with checkmark/clock icons + time ranges, "$47 spent on materials today" line, "No stops on the calendar for tomorrow" tomorrow card, salmon Sign off button dismisses cleanly). Concurrent waves M5 + M8 mid-flight on same files; surgically reset + reapplied M11 changes only to keep commit clean. |
| M9 | Mobile | ✅ PASS | `3a900eac` | 2026-05-09 | Visit edge cases (co-tech + lockbox + mid-stream cancellation): schema (`provider_visit_assignments` + `co_tech_member_ids` UUID[] / `access_method` / `access_notes` + GIN index; `handyman_requests` + `cancellation_reason` / `cancelled_at` / `cancelled_by_member_id` FK in `20261307_visit_edge_cases.sql` — `cancelled_at` was a no-op since pre-existing column). 3 new edge fn actions on `handyman-provider`: `add_co_tech` (workspace-membership validation + active-status check + non-duplicate gate, appends to array, inserts vendor-role audit message tagged `kind=co_tech_added`), `set_access_method` (4-value allowlist enforced server-side: customer_present / lockbox / key_under_mat / door_code, saves method + free-form notes), `cancel_visit_mid_stream` (closes any open pause window so paused_seconds banks correctly, stamps M9 cancellation columns + the M5 `cancelled_by_user_id` / `cancelled_by_role` columns for backward render compat, posts haven-role audit message tagged `kind=visit_cancelled` with structured metadata, optionally creates a placeholder follow-up `handyman_requests` row with `parent_request_id` + `suggested_by_request_id` linked + same-tech `provider_visit_assignments` pre-stamped at proposed_date — mirrors M8's `schedule_followup_visit` pattern; returns `request: { status, cancellationReason, cancelledAt, cancelledByMemberId }` + optional `followupRequestId`). `serializeAssignment` + `loadDashboard` extended to round-trip `coTechMemberIds` / `accessMethod` / `accessNotes` and request shape extended with `cancellationReason` / `cancelledAt` / `cancelledByMemberId`. Native iOS in `HavenFieldView.swift`: 5 new resilient-decoded fields on `HavenFieldVisitAssignment` (coTechMemberIds default `[]`, accessMethod / accessNotes default nil) and `HavenFieldVisit` (cancellationReason / cancelledAt default nil); 3 new `HavenFieldService` methods (`addCoTech` / `setAccessMethod` / `cancelVisitMidStream`); 2 new enums (`FieldAccessMethod` 4 cases with display labels + SF Symbols + notes prompts + requiresNotes predicate; `FieldCancelReason` 4 canonical reasons with free-form 'other' text branch). 5 new view computed properties on `HavenFieldVisitWorkspaceView`: `coTechSection` (rendered inline in lifecycleSection, lazy-loads workspaceMemberRoster from M7's `fetchWorkspaceMemberDirectory` on first picker open, shows roster + Add CTA), `accessMethodTile` (rendered in lifecycleSection always-visible so tech can capture access pre-arrival), `accessMethodHeaderBadge` (salmon-tinted in dark visit header — one of the load-bearing salmon usages allowed by Section 22 B1, only renders for non-default methods), `cancelledStateHeaderAnnotation` (critical-tinted, only renders when M9-tagged columns populated — distinct from M5 pre-visit homeowner cancel), 'End visit early' link in `lifecycleButtonRow` `.running` case (flat salmon text below Pause/Complete row). 3 new sheets: `FieldCoTechPickerSheet` (filters out primary tech + already-added + inactive members; B9: skeleton/empty/loaded states), `FieldAccessMethodSheet` (4-row picker + free-form notes with C1 validation when method requires notes), `FieldCancelMidStreamSheet` (4-reason picker + 'Other' free-form text with C1 empty validation + schedule-followup toggle + critical-tinted Cancel CTA). New `FieldCriticalButtonStyle` (HavenColors.critical fill — only used for the destructive cancel button) + `FieldCoTechAvatar` component. 4 new action handlers wired (`loadWorkspaceMembersIfNeeded` / `addCoTech` / `saveAccessMethod` / `cancelVisitMidStream`). Cross-app parity in Operations Desk: types.ts `VisitAssignment` extended with `techNotesCount` + `coTechMemberIds` + `accessMethod` + `accessNotes`; `VisitRow` extended with `cancellationReason` / `cancelledAt` / `cancelledByMemberId`. `VisitDetail.tsx` header pill row gains "+ N co-tech" indigo pill + "Access: Lockbox/Key/Code" warning pill (notes in tooltip); cancelled-mid-visit annotation card below pill row with Reason line; Schedule sidebar card gains "Co-tech" + "Access" rows. Helper `accessMethodLabel(method:)` maps wire-format snake_case to display labels. Verified end-to-end via curl + service-role JWT on W2 workspace: `add_co_tech` appended Crew 2 to assignment 9132f97c with audit message; `set_access_method` saved `lockbox` + 'Side gate code 1234' on the same assignment; `cancel_visit_mid_stream` cancelled request 5a1524ec mid-stream (status=cancelled, reason=weather, cancelled_at + cancelled_by_member_id stamped + M5 columns populated too), created followup request 031bd5cb at 2026-05-14T13:00:00Z with parent linkage + same-tech assignment pre-stamp + 3 audit messages on parent thread. iOS BUILD SUCCEEDED on iPhone 16e iOS 26.2 after concurrent M7 (HavenFieldCrewTab) + M10 (FieldClientsToolbarPill / FieldNearbyCustomersView / FieldBusinessCardCaptureView) WIP landed via the shared worktree; M9 isolation check passed independently. Section 22: A1 (round-trip via PostgREST + edge fn), A3 (DB cross-check via service-role JWT confirmed all 5 columns + 2 audit messages + follow-up request landed correctly), B1 (salmon header badge for load-bearing access-method context only; 'End visit early' link salmon for destructive intent; sheet primary CTAs navy except destructive cancel which uses HavenColors.critical), B3 (em-dashes scrubbed), B4 (user-facing copy uses 'visit', 'tech', 'co-tech', 'access' — never 'handyman'), B6 (44pt min on every CTA), B9 (4 states for cancel modal + picker), C1 (empty 'Other' fires inline validation, lockbox/key/code without notes fires '\"Add the lockbox details so the next tech knows.\"'), D1 (skeleton on roster load, never spinner-on-blank), E1 (lifecycleAssignment override + binding-based sheet state preserves across background→foreground). Concurrent M7+M10 WIP shared the file; pulled atomically before commit, surgical add of just my files (HavenFieldView.swift / handyman-provider/index.ts / types.ts / VisitDetail.tsx / migration). Commit `3a900eac`. |
| M8 | Mobile | ✅ PASS | `4ec5d93d` | 2026-05-08 | End-of-visit suggestion authoring (drives recurring revenue): 3 new edge fn actions (`suggest_followup_task` writes `maintenance_tasks` row with `source='contractor_suggestion'` + `suggested_by_request_id` + `suggested_by_workspace_id` + audit message tagged `metadata.kind='task_suggested'` + homeowner push, defaults `next_due_date` to today+30 when none provided to satisfy NOT NULL; `suggest_followup_quote` creates a draft `provider_quotes` row with `contractor_id` pulled from parent request + auto-generated `public_share_token` + `suggested_by_request_id` stamped, returns id so the iOS sheet can hand off to M4's BuildQuoteSheet; `schedule_followup_visit` creates a fresh `handyman_requests` row with `parent_request_id` + `suggested_by_request_id` + `proposed_by_role='handyman'`, pre-stamps a `provider_visit_assignments` slot for the suggesting tech with computed next stop_order, posts a 'haven'-role audit message ('We scheduled a follow-up visit on Sun, May 10 at 1:00 PM') + homeowner push). Schema (`20261306_followup_suggestions.sql`): `maintenance_tasks` + `source` / `suggested_by_request_id` / `suggested_by_workspace_id` / `suggested_at` + partial index; `provider_quotes` + `suggested_by_request_id` / `suggested_at` + index; `handyman_requests` + `suggested_by_request_id` (self-FK alongside existing `parent_request_id`) / `suggested_at` + index. Native iOS in `HavenFieldView.swift`: `FieldEndOfVisitWizard` sheet auto-presents on first `.completed` lifecycle transition (per-visit `havenfield.m8.wizardShown.<requestId>` AppStorage gate prevents re-fire), reachable post-dismiss via 'Send follow-up suggestions' secondary CTA. Wizard shape: Wrap-up nav title + 'ANYTHING TO FOLLOW UP ON?' kicker + free-text Observation TextEditor (saves to visit's tech notes via existing M6 surface) + three '+ Task / + Quote / + Visit' chip row that toggles inline forms below + Suggestions queue with kind pills + Remove buttons + single salmon 'Done — send suggestions' CTA + Skip link in nav. Templated quick-picks for tasks (Replace HVAC filter / Reseal valve / Annual furnace tune-up / Inspect water heater anode / Replace smoke detector batteries / Service well pressure tank). Quick-pick slots for visits (tomorrow 9am / day-after 1pm / 3 days out 9am) + DatePicker fallback + duration segmented picker (30/45/60/90/120 min). E1 background→foreground state preservation via `havenfield.m8.wizardDraft.<requestId>` AppStorage JSON cache (observation + queue restore on re-foreground). 3 new `HavenFieldService` methods (`suggestFollowupTask` / `suggestFollowupQuote` / `scheduleFollowupVisit`). Submit walks queue tasks-first then quotes then visits; if any quote staged, hands off to M4's BuildQuoteSheet over the most recent draft for line-item editing + signature; otherwise fires 3-second 'Suggestions sent to the homeowner.' success banner on visit-complete screen and dismisses. Cross-app parity: Operations Desk Quotes (`Quotes.tsx`) renders 'SUGGESTED' indigo pill next to BUNDLE pill when `q.suggestedByRequestId` non-null; Operations Desk Visits Dispatch (`Visits.tsx`) renders 'Suggested by visit' indigo pill on rows with `suggestedByRequestId` set (pairs with existing 'Routed by Chez' pill so operator distinguishes homeowner-submitted vs admin-routed vs tech-suggested); types extended in `lib/types.ts`. Suggested tasks land in `maintenance_tasks` visible on the homeowner iOS Tasks tab automatically (no thread-renderer change needed). Verified end-to-end on iPhone 16e iOS 26.2 with W2 owner session: started visit → tapped Complete → wizard fired with empty state → pasted observation → '+ Task' → picked 'Reseal the valve before next service' template → Add to list → row appeared in queue → Done → wizard dismissed cleanly. DB cross-check via service-role JWT confirmed maintenance_tasks row landed with all M8 fields, audit message inserted on parent thread with metadata.kind='task_suggested', tech-note row inserted with 'Observation: ...' body. Re-entered visit → wizard did NOT re-fire (per-visit gate held). Hit two transient deploy collisions with concurrent M5/M11 waves on same files; merged conflict regions surgically and rebuilt clean. Section 22: A1 (queue persists via AppStorage), A3 (DB cross-check passed), B1 (only Done is salmon — chips/forms/queue all navy/outline), B3 (no em dashes in user-facing copy), B4 (no 'handyman' leaks; 'follow-up' / 'next visit' / 'quote draft'), B6 (44pt min on every chip + queue Remove + slot row), B9 (loading/empty/error/populated all wired), C1 (empty title fires inline 'Add a task title before staging.' validation with salmon-bordered field), D1 (no spinners on blank), E1 (background→foreground state preservation via per-visit AppStorage). |

## Vercel deploy state (last checked 2026-05-08 ~3:40 PM ET)

- `https://www.getchez.com/operations/` → **200** (Operations Desk SPA)
- `https://www.getchez.com/handyman-visit.html` → **404** (PWA retired ✓)
- `https://www.getchez.com/handyman.html` → **200** (legacy auth pitch still working)

## iOS Simulator state

- iPhone 16e (id `F9946648-90C6-42B0-AFCB-92E3807F36C8`, iOS 26.2) booted with ChezField.app installed + signed in as W2 owner.
- Field workspace renders correctly — Overview tab shows "Good afternoon, E2E", 9 homeowner requests, route summary card.
- Auth session persists in UserDefaults via `HavenSimulatorAuthStorage` (commit `c1248a01`).

## Schema migrations applied this pass

- `20261301_visit_lifecycle.sql`
- `20261302_punch_capture_depth.sql`
- `20261303_home_system_decommission.sql`
- `20261304_quote_signature.sql`
- `20261305_visit_tech_notes.sql`
- `20261306_followup_suggestions.sql`
- `20261307_visit_edge_cases.sql`
- `20261308_crew_chat.sql`
- `20261309_part_requests.sql` (applied via `supabase db query --linked` due to pre-existing 20261212 dupe in remote schema_migrations)

## Deferred to follow-up waves

Captured in detail in the per-wave JSON outputs. High-priority deferred items:

- **M6 route_summary stub** — currently a 15-min-between-stops + 10-min-initial-leg estimate. Future wave: swap in Apple MapKit ETARequest or Google Directions API.
- **M6 reschedule quick-slot stub** — picks tomorrow 9am, day-after 1pm, 3 days out 9am. Future: real open-windows lookup against the tech's calendar.
- **M6 customer-phone pull-through** — `FieldTappablePhoneRow` is wired but property-summary shape doesn't include phone yet; needs join through `users.phone` or `family_members.phone` in `loadDashboard`.

## What's next

Per the orchestrator's pass plan, **Pass 2 — Web foundation** (B7 → W2 → W3 → W18) is the next block. Mobile Pass 2 (M4 → M5 → M8 → M11) covers the remaining sales + close-of-day flows.

Demo readiness for the mobile contractor surface: **GO** (all four mobile-foundation waves clean, sign-in works, native iOS surfaces are field-ready).
