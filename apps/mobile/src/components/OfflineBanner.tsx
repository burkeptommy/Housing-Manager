// =============================================================================
// OFFLINE BANNER STUB
// @react-native-community/netinfo is temporarily stubbed.
// The native module is not linked in Expo managed workflow without a config plugin.
// TODO: Add NetInfo Expo config plugin when ready
// =============================================================================

/**
 * OfflineBanner - Stubbed version
 *
 * @react-native-community/netinfo requires a config plugin that is not yet set up.
 * This stub returns null to prevent crashes. Network status detection will be
 * added in a future update.
 */
export function OfflineBanner() {
  // Stubbed - NetInfo native module not available
  // TODO: Re-enable when @react-native-community/netinfo config plugin is added
  return null;
}
